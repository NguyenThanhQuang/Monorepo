// Common Enums
export enum SortOrder {
  ASC = "asc",
  DESC = "desc",
}

// TODO: Move other enums (UserRole, etc.) here from legacy code
export enum UserRole {
  ADMIN = "admin",
  USER = "user",
  STAFF = "staff",
}
